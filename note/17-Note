State
	状态
	并不是Flink独创或者特有的

	socket  wc
		进来一条数据，就计算一次
		再进来一条，update  insert
		pk,pk
			(pk,1)
			(pk,2)
		zs
			(zs,1)
		pk
			(pk,3)		

	当前计算流程需要依赖之前计算的结果，那么之前计算的结果就是状态


	DAU  id做去重  就需要把前面已有的id给我存下来，否则我咋去重
















